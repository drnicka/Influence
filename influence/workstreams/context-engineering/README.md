# context-engineering
Getting intent from the user's mind into the context window with weights
attached. Projects: `readback/` (context endpoints agents consume),
`boot-pack/` (this influence/ folder and the fresh-agent boot test).
