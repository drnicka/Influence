# Workstreams

The file system is the ontology. Each folder is a workstream; projects nest
inside; each level's README states intent in a few lines. Scan this tree to
orient before proposing ballots; extend it as votes create new work.

Current workstreams (seeded from the 2026-07-07 votes):
- `context-engineering/` — the core loop: readback surfaces, boot pack
- `curation-lifecycle/` — triage feedback, reopen/membership verbs, rounds
- `agent-consensus/` — perspective agents, rooms, consensus mechanics
- `repo/` — docs, branch strategy, hygiene
