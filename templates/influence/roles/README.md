# Perspective roles

Default consensus voters. Each file defines a lens, priors, and voting
temperament. Spawn one agent per role with its file as the system framing,
plus a member key. Agents vote independently and must not hedge to the middle.
Users craft their own roles on top of this default set.
