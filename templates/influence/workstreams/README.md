# Workstreams

The file system is the ontology: each folder is a workstream, projects nest
inside, every level's README states intent in a few lines. This tree is the
rapid context-direction path — scan it before proposing ballots.

Folders here exist because a vote created them. If this folder is empty, run
the Spawn protocol in `../ORIENTATION.md`:
1. **Vote 1 — repo priorities**: what matters first, in plain language.
2. **Vote 2 — workstream folders**: each card is a folder; + creates it,
   − declines it; weights set the order of attention.

Index (ordered by weight, updated after each ontology vote):
_none yet_
