# Agent README

Moved: the agent protocol lives in **`influence/ORIENTATION.md`** (one page —
register, push ballots, read weighted context back), with conventions in
`influence/CONVENTIONS.md` and consensus roles in `influence/roles/`.

The pre-fork VPS/router runbooks this file used to hold are preserved on
`main` and in git history.
